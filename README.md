# BS Oils Website

A React website with CSS Modules, a home page, and product subpages for oil offerings.

## Setup

1. `npm install`
2. `npm start`

## Structure

- `src/components` — reusable UI pieces with module CSS
- `src/pages` — routed page views
- `src/data` — oil product content
